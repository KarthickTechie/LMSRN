import { View, Text } from "react-native";
import React from "react";

const SubHeader = () => {
  return (
    // <Text>{props.title}</Text>
    <View className="flex w-[100vw] h-[20px] bg-navy"></View>
  );
};

export default SubHeader;
